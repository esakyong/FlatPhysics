#pragma once


#ifndef PI
	#define PI 3.14159265358979323846f
#endif

namespace FlatPhysics
{
	namespace FlatConstants
	{
	
		const float MinBodySize = 0.1f * 0.1f; 
		const float MaxBodySize = 640.0f * 640.0f; 

		const float MinDensity = 0.5f; // g/cm^3
		const float MaxDensity = 21.4f; 

		

	};
}